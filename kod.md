client.on("ready", () => {
      setInterval(() => {
       let wasty = client.channels.cache.get("atacağı kanal id") // zoo


      wasty.send("owo zoo")


      }, 45000);
})



//main masaj atma kodu
